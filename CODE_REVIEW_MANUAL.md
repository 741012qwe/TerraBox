# 工程师级代码审查完整操作手册

> 适用场景：生产环境上线前代码审查、技术债评估、关键模块重构

---

## 第一部分：分步对话流程（推荐新手或复杂项目）

按顺序发送以下指令，每步之间等待AI回复再继续。

### 步骤1：启动审查

```
你是我团队的首席架构师，有10年大厂生产环境经验，性格极度严谨偏执。请严格对照《Google工程实践指南》和《代码整洁之道》，拒绝任何'能跑就行'的妥协。

输出格式要求：生成一份包含 [总体评分]、[P0严重缺陷]、[P1一般问题]、[P2优化建议]、[重构后的核心代码片段] 的结构化报告。最后用一句话总结：'这段代码上线后，我最可能在凌晨3点被叫起来的原因是什么？'
```

### 步骤2：核心四维检查

#### 第一维：正确性与鲁棒性

```
请检查所有边界条件（null、空集合、超长字符串、并发冲突），并逐行列出代码作者做出的所有隐含假设，按类型分类：

· 数据假设（如默认输入非空、格式合法）
· 环境假设（如网络畅通、磁盘可写、时区一致）
· 时序假设（如A操作一定先于B）
· 幂等假设（如请求不会重复）

对每条假设，给出一个破坏性场景，并用断言或防护代码加固。
最后，给出一个能导致程序崩溃的最小可复现输入示例。
```

#### 第二维：性能与扩展性

```
推导代码的时间复杂度，指出最慢路径。若数据量增长100倍，哪里先成为瓶颈？

重点检查：
①数据库查询是否存在N+1或死锁风险
②内存是否可能泄漏或连接未释放
③是否存在不必要的循环或重复计算

请给出空间换时间或惰性加载的改造方案。
```

#### 第三维：安全性与攻击面

```
从OWASP Top 10视角攻击这段代码。重点检查：SQL/命令注入、失效的身份认证、敏感数据（密码/密钥）泄露、SSRF（服务端请求伪造）、不安全的反序列化。

若发现漏洞，请指出攻击者利用此漏洞能获取的最高权限，并演示攻击过程，最后给出防御补丁。
```

#### 第四维：可维护性与整洁度

```
评估圈复杂度，若>10则建议重构。

重复代码专项：不要只找拷贝粘贴的重复，请检测语义结构相似的代码块（如相同流程但操作不同对象）。按严重程度分级：

· A级：同一逻辑出现≥3次，必须抽象
· B级：骨架相同，适合模板方法或策略模式
· C级：硬编码字符串/数字多处重复

针对A/B级，用设计模式给出合并后的代码，并对比重构前后的代码行数和维护点数。同时评估你的抽象方案是否过度设计——如果未来新增一种类型，需改动几个文件？要求少于2个。
```

### 步骤3：追问与对赌

```
1. 请用数学归纳法或循环不变式证明你修改后的逻辑始终成立。

2. 若你的修改引入了新第三方库，请评估其许可证兼容性和社区活跃度，并给出回滚方案。
3. 如果我不采纳你的P1建议，请帮我写一段技术债务注释（TODO），注明'在什么QPS阈值下必须修复'。
```

### 步骤4：交付验收

```
请基于最终代码，自动生成三样产出：
①变更日志（Changelog）
②单测补充建议（指出未覆盖的分支）
③灰度发布监控指标（建议的告警阈值和需要关注的关键日志关键字）
```

---

## 第二部分：万能一次性粘贴模板

时间紧张时，直接复制整段指令（替换`[你的代码]`即可）：

```
请对以下代码进行生产级工程审查，必须包含以下所有专项，最终输出结构化报告：

代码：
[你的代码]

审查要求：
1. 【正确性与隐含假设】穷举所有边界条件（null、空、超长、并发），列出所有数据、环境、时序、幂等假设，并给出破坏性场景和防御断言。同时提供最小崩溃输入示例。

2. 【性能与资源】推导时间复杂度，指出100倍数据量下的瓶颈。检查数据库N+1、内存泄漏、连接未释放，给出优化方案（空间换时间/惰性加载）。

3. 【安全与攻击】按OWASP Top 10检查注入、身份认证、敏感泄露、SSRF等，演示攻击路径并给出补丁。

4. 【可维护性与重复代码】评估圈复杂度，若>10则重构。检测语义相似的重复代码块（非仅文本相同），按A/B/C级分类，用设计模式消除重复，并评估是否过度设计（新增类型改动少于2个文件）。

5. 【最终交付】基于修改后的代码，生成变更日志、单测补充建议（列未覆盖分支）、以及灰度监控指标（告警阈值和日志关键字）。

额外要求：所有修改必须附带解释原理，并给出5个我未想到的极端测试用例。
```

---

## 使用建议

| 场景 | 推荐方式 |
|------|----------|
| 首次使用/复杂项目 | 分步流程（逐层深挖） |
| 时间紧张 | 一次性模板 + 针对性追问 |
| 特定语言 | 模板开头加：`特别注意[语言]特有的坑，如Goroutine泄漏、Python GIL、JS闭包引用等` |
| 微服务分布式事务 | 追加：`检查分布式锁、最终一致性、幂等性、Saga补偿机制` |
| 嵌入式C | 追加：`检查指针解引用、栈溢出、中断上下文安全性、硬件寄存器访问原子性` |
| 机器学习训练脚本 | 追加：`检查数据泄漏、GPU内存管理、断点恢复、分布式训练同步` |

---

## 实战案例：TerraBox Minecraft插件（2026-09-02）

### 发现问题

| 级别 | 问题 | 位置 |
|------|------|------|
| P0 | 线程池泄漏，多次切换地形后OOM | `WorldService.pregenWorld()` L226 |
| P0 | BlockPopulator调用sync API触发StackOverflow | `CustomTerrainGenerator.SparseTreePopulator` |
| P1 | 哈希常量格式错误导致编译警告 | `mix64()` 方法 |

### 修复方案

```java
// 修复1: 线程池管理
private void cancelPregen(PregenState st) {
    if (st.executor != null) {
        st.executor.shutdownNow();
        st.executor.awaitTermination(5, TimeUnit.SECONDS);
    }
}

// 修复2: 避免sync API，改用确定性哈希
long hash = mix64(wx * 31L + wz);
boolean isDesert = (hash & 0xFF) < 60; // 23.5%概率
```

### 验证清单

- [ ] 重启后无StackOverflowError
- [ ] 多次切换地形无线程泄漏
- [ ] 地图正常生成，树木分布符合预期
- [ ] 预生成速度≥2万区块/秒

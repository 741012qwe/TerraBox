# TerraBox 物资大陆 v2.2.0
# 数据库存储配置

database:
  # 数据库类型: sqlite (默认) 或 mysql
  type: sqlite
  
  # SQLite 配置
  sqlite:
    path: data/database.db
  
  # MySQL 配置
  mysql:
    url: jdbc:mysql://localhost:3306/terrabox
    database: terrabox
    username: root
    password: ""
    ssl: false
